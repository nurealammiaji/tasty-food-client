import { Helmet } from "react-helmet-async";

const Reservation = () => {
    return (
        <div>
            <Helmet>
                <title>Reservation || Tasty Food</title>
            </Helmet>
            <div className="p-10">
                <div>

                </div>
            </div>
        </div>
    );
};

export default Reservation;
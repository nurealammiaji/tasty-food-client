import { Helmet } from "react-helmet-async";

const AddReview = () => {
    return (
        <div>
            <Helmet>
                <title>Add Review || Tasty Food</title>
            </Helmet>
            <div className="p-10">
                <div>

                </div>
            </div>
        </div>
    );
};

export default AddReview;